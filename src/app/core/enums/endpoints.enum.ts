export enum AUTH_ENDPOINT {
    LOGIN = '/api/auth/login',
    REGISTER = '/api/auth/register',
    LOGOUT = '/api/auth/logout',
    PROFILE = '/api/auth/profile'
}
export enum NEWS_ENDPOINT {
    NEWS = '/api/news'
}
export enum USER_ENDPOINT {
    USER = '/api/user'
}
